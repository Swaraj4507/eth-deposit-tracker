services:
  influxdb:
    image: influxdb:2.0
    container_name: influxdb
    ports:
      - "8086:8086"
    env_file:
      - influxdb.env
    volumes:
      - influxdb-storage:/var/lib/influxdb2
volumes:
  influxdb-storage: